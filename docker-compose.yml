version: '3.8'
services:
  app:
    build: .
    container_name: mas_app
    ports:
      - "8000:8000"
    env_file:
      - .env
    # Если на Linux host.docker.internal не работает, раскомментируйте:
    # extra_hosts:
    #   - "host.docker.internal:host-gateway"
    depends_on: []

# Сервис db закомментирован, чтобы не перезаписывать боевую БД
#  db:
#    image: postgres:15
#    environment:
#      POSTGRES_USER: ${DB_USER}
#      POSTGRES_PASSWORD: ${DB_PASSWORD}
#      POSTGRES_DB: ${DB_NAME}
#    volumes:
#      - ./db/initdb:/docker-entrypoint-initdb.d
#      - pgdata:/var/lib/postgresql/data

#volumes:
#  pgdata:
